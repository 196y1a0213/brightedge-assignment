# Application Configuration
# Copy this file to .env and adjust as needed
# Note: All settings are optional with sensible defaults

# Application Settings
APP_NAME="Page Classification System"
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8000
TIMEZONE=UTC

# Database Settings (Not currently used, but ready for future features)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=brightedge
DB_USERNAME=root
DB_PASSWORD=

# Logging
LOG_LEVEL=debug
LOG_PATH=logs/app.log
